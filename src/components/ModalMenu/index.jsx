import './index.css';

const ModalMenu = ({ isMenuOpen }) => {
    return (
        <div className="modal-menu" style={{ display: isMenuOpen ? 'block' : 'none' }}>
            
        </div>
    )
}

export default ModalMenu